#include "main.h"
#include <stdio.h>

/**
* main - Entry point
*Description: Calls all functions
*
*Return: Always 0
*/

int main(void)
{
	/**
	*char c = 'w';
	*
	int num = '3';

	*printf("%c: %d\n", c, _isupper(c));
	*

	*printf("%d: %d\n", num, _isdigit(num));
	
	*int a = 30;

	*int b = 10;

	*printf("%d", mul(a, b));
	*
	*print_numbers();
	*
	print_most_numbers();
	*
	*more_numbers();
	*
	*print_line(4);
	*
	*print_diagonal(0);
	*
	*print_square(20);
	*
	*print_triangle(2);
	*fizz_buzz();
	*/
	print_number(-90);

	return (0);
}
