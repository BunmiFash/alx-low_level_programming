#include "main.h"
/**
 * _islower - Checks for lowercase characters
 * @c: Characters; a - z
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);

}
