#include <stdio.h>
/**
 * main - Entry point
 * Description: prints  all single digits
 *Return: 0
 */

int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
		printf("%d", digit);
	}
	return (0);
}
